<a href="cards/new">New</a>

<ul>
    <% for( let i=0; i < results.length; i++ ) { %>
        <li>
            <%= results[i].name %>
                <img src="<%= results[i].images.small %> " />
        </li>
        <% } %>
</ul>